<!DOCTYPE html>
<html>
  <head>
    <%= render 'shared/head' %>
  </head>

  <body>
    <%= render 'shared/navbar' %>
    <%= render 'shared/notices' %>

    <div class="container pb-5">
      <%= yield %>
    </div>

    <%= render 'shared/footer' if params[:account_id].blank? %>
  </body>
</html>
